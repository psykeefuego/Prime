#include<stdio.h>
void main()
{
	int n,i,d,num=0;
	printf("Enter a number \n");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		d=n%i;
		if(d==0)
		{
		num++;	
		}
	}
	if(num==2)
	{
		printf("\n %d is a prime number",n);
	}
	else
	{
		printf("\n %d is not a prime number",n);
	}
}
